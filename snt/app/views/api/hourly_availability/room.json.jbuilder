json.availability  do 
  json.old_rate_amount @old_rate_amount
  json.old_room_id @old_room_id
  json.old_rate_id @old_rate_id
  json.new_rate_amount @new_rate_amount
  json.is_available @is_available
  json.message @message
end

